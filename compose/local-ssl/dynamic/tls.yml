# Add your local certificate here...
# cd this/folder
# mkcert -install
# mksert "*.domain.local"
tls:
  certificates:
    - certFile: /etc/certs/orders.local.pem
      keyFile: /etc/certs/orders.local-key.pem
